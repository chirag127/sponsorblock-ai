name: release-notes
on:
  release:
    types: [created]
  workflow_dispatch:
jobs:
  release-notes:
    uses: chirag127/workflows/.github/workflows/release-notes.yml@main
    secrets: inherit
    permissions:
      contents: write
      pull-requests: read
